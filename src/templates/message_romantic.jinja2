{# 序幕 #}
{{ scene }}
今天是 {{ day_status.date.strftime("%Y年%m月%d日") }} · 周{{ day_status.weekday_cn_short }}
岁在{{ lunar_date.strftime("%o年(%a)") }}缓缓流转，月色落在{{ lunar_date.strftime("%L%M月%D") }}之间。{{ greeting }}
{# 衔接与过渡 #}
{{ tone.header }}
{# 数据 #}
> ⏳{{ year_progress_status.render_wecom_html() }}
{# 周末倒计时 #}
{% if weekend_status.friday.is_today %}
> 💫今天周五，{% if weekend_status.tomorrow.is_workday -%}
明天还要奔赴日常，但今晚可以慢一点🌙
{% else -%}
明天开始属于自己了🎉
{% endif %}
{% elif weekend_status.friday.days_until > 0 %}
> 🌿周五正在靠近，还差<font color="{% if weekend_status.friday.days_until <= 3 %}warning{% else %}info{% endif %}">{{ weekend_status.friday.days_until }}</font>场日落
{% endif %}
{# 节日提醒 #}
{% for festival in festivals -%}
> 🎈**{{ festival.name }}**
{%- if festival.days <= 7 -%}
已近在咫尺，再过<font color="warning">{{ festival.days }}</font>个夜晚便能相见
{% elif festival.days <= 31 -%}
正在靠近，还差<font color="info">{{ festival.days }}</font>次月色流转
{% elif festival.days <= 185 -%}
还在远方，还差<font color="comment">{{ festival.days }}</font>缕微风的照面
{% else -%}
蓄满了温柔，在<font color="comment">{{ festival.days }}</font>场朝阳之外静静守候
{% endif %}
{% endfor %}

{# 收尾 #}
{{ tone.footer }}
{%- if one_sentence %}

「{{ one_sentence.get('content', '') }}」
 —— {{ one_sentence.get('author', '未知') }}
{%- endif %}