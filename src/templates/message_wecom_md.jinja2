[🐟🐟🐟] 提醒您: {{ today_status.date.strftime("%Y年%m月%d日") }}{{ today_status.weekday_cn }} · {{ lunar_date.strftime("%o年(%a) %L%M月%D") }}, {{ greeting }}~ 摸鱼人
{{ header_text }}
{# 年进度条 #}
> {{ year_progress_status.render_wecom_html() }}
{# set length = 10 #}
{# set filled_count = (year_progress_status.progress / 100 * 10) | int #}
{# set empty_count = 10 - filled_count #}
{# set filled_bar = "█" * filled_count #}
{# set empty_bar = "░" * empty_count #}
{# if year_progress_status.progress < 30 #}
{# set percent_color = "info" #}
{# elif year_progress_status.progress < 85 #}
{# set percent_color = "warning" #}
{# else #}
{# set percent_color = "comment" #}
{# endif #}
{# <font color="warning">{{ filled_bar }}</font><font color="comment">{{ empty_bar }}</font> <font color="{{ percent_color }}">{{ "%.2f"|format(year_progress_status.progress) }}%</font> #}
{# 周末倒计时 #}
{% if workweek_status.friday.is_today %}
> 今天是周五 {% if workweek_status.tomorrow.is_workday -%}
明天还得上班😭
{% else -%}
明天休息啦🎉
{% endif %}
{% elif workweek_status.friday.days_until > 0 %}
> 距离周五还有<font color="{% if workweek_status.friday.days_until <= 3 %}warning{% else %}info{% endif %}">{{ workweek_status.friday.days_until }}</font>天
{% endif %}
{# 节日提醒 #}
{% for festival in festivals %}
{% if festival.days <= 3 %}
> 距离{{ festival.name }}还有<font color="warning">{{ festival.days }}</font>天
{% elif festival.days <= 7 %}
> 距离{{ festival.name }}还有<font color="info">{{ festival.days }}</font>天
{% else %}
> 距离{{ festival.name }}还有<font color="comment">{{ festival.days }}</font>天
{% endif %}
{% endfor %}

{% if one_sentence %}
{{ one_sentence.get('content', '') }}
—— {{ one_sentence.get('author', '未知') }}「{{ one_sentence.get('source', '网络') }}」
{% else %}
{{ footer_text }}
{% endif %}